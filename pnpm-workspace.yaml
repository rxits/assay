packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  # msw's postinstall only writes the browser service-worker; we use msw/node.
  msw: false
  prisma: true
